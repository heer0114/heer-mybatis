<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="me.heer.mybatis.mapper.TUserMapper">
	<resultMap id="BaseResultMap" type="me.heer.mybatis.entity.TUser">
		<!-- WARNING - @mbggenerated This element is automatically generated by 
			MyBatis Generator, do not modify. -->
		<result column="id" property="id" />
		<result column="user_name" property="userName" />
		<result column="real_name" property="realName" />
		<result column="sex" property="sex" />
		<result column="mobile" property="mobile" />
		<result column="email" property="email" />
		<result column="note" property="note" />
		<result column="position_id" property="positionId" />
	</resultMap>
	<insert id="insert" parameterType="me.heer.mbstudy.model.TUser">
		<selectKey resultType="java.lang.Integer" keyProperty="id"
			order="BEFORE">
			SELECT LAST_INSERT_ID()
		</selectKey>
		insert into t_user (id, user_name, real_name,
		sex, mobile, email,
		note, position_id)
		values (#{id,jdbcType=INTEGER}, #{userName,jdbcType=VARCHAR},
		#{realName,jdbcType=VARCHAR},
		#{sex,jdbcType=TINYINT}, #{mobile,jdbcType=VARCHAR}, #{email,jdbcType=VARCHAR},
		#{note,jdbcType=VARCHAR}, #{positionId,jdbcType=INTEGER})
	</insert>

	<select id="selectAll" resultMap="BaseResultMap">
		select id, user_name, real_name, sex, mobile, email, note, position_id
		from t_user
	</select>

	<select id="selectById" resultMap="BaseResultMap">
		select id, user_name, real_name, sex, mobile, email, note, position_id
		from t_user where id = ?
	</select>
</mapper>